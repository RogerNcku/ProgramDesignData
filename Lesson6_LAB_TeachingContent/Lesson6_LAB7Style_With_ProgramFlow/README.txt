Lesson 6 LAB7Style With Program Flow

已整合主題 1–16。各子目錄 index.html 已移除，請從最外層 index.html 進入。每頁包含可編輯 C 程式碼、線上編輯器連結、程式流程圖與流程講解。


本版本已將各 topic HTML 的投影片圖片改為逐張下拉式詳細講解區塊：每張投影片皆包含圖片、核心觀念、學習重點與一句話記憶。

中文修正版更新紀錄
1. 已在所有 C 程式範例加入中文註解，包含資料結構、主要函式、走訪/搜尋/排序/Heap 操作流程。
2. 已同步更新各 HTML 內嵌的 <code> 與 <textarea> 程式碼，網頁上看到的程式碼與下載 .c 檔一致。
3. 已修正動畫互動問題：移除重複載入 script.js，避免全域變數重複宣告造成按鈕失效。
4. 已補齊缺少的 JavaScript 互動函式，例如 binaryFocus、arrayFocus、runTraversal、searchBST、stepBuild、showDeleteCase、nextBstSort、demoSearch、nextHeap 等。
5. 已修正 copyCode，可同時支援 textarea 與 code 區塊，避免複製按鈕無效。
6. 已補強互動動畫 CSS，高亮、隱藏/顯示與節點狀態更穩定。
